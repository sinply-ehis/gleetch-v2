import { hsl2rgb, pixelFill, smoothstep } from '../core/color.js';
import { fbm } from '../core/rng.js';

// Each pattern receives (ctx, W, H, rng) and draws directly to ctx.
// Randomness is derived from rng() so output is seed-reproducible.


export function p_marble(ctx,W,H,rng){const bh=rng()*360,sd=rng()*9999|0;pixelFill(ctx,W,H,(_,__,nx,ny)=>{const n=fbm(nx,ny,sd,6,3)+fbm(nx+1,ny,sd+1,4,5)*.5,v=Math.sin((nx*4+n*3)*Math.PI)*.5+.5;return hsl2rgb((bh+v*60)%360,30+v*40,30+v*50);});}
export function p_wood_grain(ctx,W,H,rng){const bh=25+rng()*30,sd=rng()*9999|0;pixelFill(ctx,W,H,(_,__,nx,ny)=>{const d=fbm(nx*.5,ny*.5,sd,4,2)*.15,dist=Math.sqrt((nx-.5+d)**2+(ny-.5+d)**2),ring=Math.sin(dist*30+fbm(nx,ny,sd+1,3,3)*1.5)*.5+.5;return hsl2rgb(bh+ring*20,60,20+ring*40);});}
export function p_camouflage(ctx,W,H,rng){const sd=rng()*9999|0,clrs=['#4a5240','#6b7a5e','#8a9e70','#3d4a35','#2c3626'];pixelFill(ctx,W,H,(_,__,nx,ny)=>{const v=fbm(nx,ny,sd,4,2),ci=Math.floor(v*clrs.length)%clrs.length,hex=clrs[ci];return[parseInt(hex.slice(1,3),16),parseInt(hex.slice(3,5),16),parseInt(hex.slice(5,7),16)];});}
export function p_bubbles(ctx,W,H,rng){const bh=rng()*360,n=30+rng()*40|0;ctx.fillStyle=`hsl(${bh},50%,85%)`;ctx.fillRect(0,0,W,H);for(let i=0;i<n;i++){const x=rng()*W,y=rng()*H,r=10+rng()*50,h=(bh+rng()*80)%360,g=ctx.createRadialGradient(x-r*.3,y-r*.3,r*.1,x,y,r);g.addColorStop(0,`hsla(${h},60%,98%,0.9)`);g.addColorStop(.6,`hsla(${h},50%,80%,0.3)`);g.addColorStop(1,`hsla(${h},60%,50%,0.6)`);ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fillStyle=g;ctx.fill();ctx.strokeStyle=`hsla(${h},60%,70%,0.7)`;ctx.lineWidth=1.5;ctx.stroke();}}
export function p_cells(ctx,W,H,rng){const bh=rng()*360,n=25+rng()*35|0,pts=Array.from({length:n},()=>({x:rng()*W,y:rng()*H,h:(bh+rng()*60)%360}));pixelFill(ctx,W,H,(x,y)=>{let d1=Infinity,d2=Infinity,bi=0;for(let i=0;i<pts.length;i++){const d=(x-pts[i].x)**2+(y-pts[i].y)**2;if(d<d1){d2=d1;d1=d;bi=i;}else if(d<d2)d2=d;}const edge=smoothstep(0,1,1-Math.min(1,Math.abs(Math.sqrt(d1)-Math.sqrt(d2))/5));if(edge>.75)return[8,8,12];return hsl2rgb(pts[bi].h,60,35+edge*20);});}
export function p_coral(ctx,W,H,rng){const bh=rng()*360;ctx.fillStyle=`hsl(${(bh+180)%360},60%,15%)`;ctx.fillRect(0,0,W,H);function branch(x,y,ang,len,depth){if(depth===0||len<2)return;const x2=x+Math.cos(ang)*len,y2=y+Math.sin(ang)*len;ctx.strokeStyle=`hsl(${(bh+depth*20)%360},80%,${40+depth*5}%)`;ctx.lineWidth=depth*.7;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x2,y2);ctx.stroke();const sp=.3+rng()*.4;branch(x2,y2,ang-sp,len*.7,depth-1);branch(x2,y2,ang+sp,len*.7,depth-1);if(rng()<.4)branch(x2,y2,ang,len*.6,depth-1);}for(let i=0;i<2+rng()*3|0;i++)branch(rng()*W,H,-Math.PI/2+(rng()*.4-.2),50+rng()*30,8);}
export function p_crystal(ctx,W,H,rng){const bh=rng()*360;ctx.fillStyle=`hsl(${bh},40%,10%)`;ctx.fillRect(0,0,W,H);function grow(x,y,ang,len,depth){if(depth===0||len<3)return;const x2=x+Math.cos(ang)*len,y2=y+Math.sin(ang)*len;ctx.strokeStyle=`hsla(${(bh+depth*30)%360},80%,${50+depth*5}%,0.8)`;ctx.lineWidth=depth*.5+.5;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x2,y2);ctx.stroke();for(let i=0;i<2+rng()*2|0;i++)grow(x2,y2,ang+(rng()-.5)*Math.PI*.7,len*(.5+rng()*.2),depth-1);}for(let i=0;i<3+rng()*4|0;i++)grow(rng()*W,rng()*H,rng()*Math.PI*2,30+rng()*40,7);}
export function p_lightning(ctx,W,H,rng){const bh=rng()*360;ctx.fillStyle='#030308';ctx.fillRect(0,0,W,H);function bolt(x,y,tx,ty,w,depth){if(w<.3||depth>9)return;const mx=(x+tx)/2+(rng()-.5)*(Math.abs(tx-x)+Math.abs(ty-y))*.35,my=(y+ty)/2+(rng()-.5)*(Math.abs(tx-x)+Math.abs(ty-y))*.35;ctx.strokeStyle=`hsla(${(bh+w*20)%360},90%,${60+w*10}%,${.3+w*.1})`;ctx.lineWidth=w;ctx.beginPath();ctx.moveTo(x,y);ctx.quadraticCurveTo(mx,my,tx,ty);ctx.stroke();bolt(x,y,mx,my,w*.7,depth+1);bolt(mx,my,tx,ty,w*.7,depth+1);if(rng()<.4)bolt(mx,my,mx+(rng()-.5)*100,my+(rng()-.5)*100,w*.4,depth+1);}for(let i=0;i<2+rng()*3|0;i++)bolt(rng()*W,0,rng()*W,H,4+rng()*3,0);}
export function p_cracks(ctx,W,H,rng){const bh=rng()*360,sd=rng()*9999|0;pixelFill(ctx,W,H,(_,__,nx,ny)=>{const v=fbm(nx,ny,sd,5,3),v2=fbm(nx,ny,sd+1,5,3);if(Math.abs(v-v2)<.015)return[5,5,10];return hsl2rgb((bh+v*60)%360,40,30+v*40);});}
export function p_sand_dunes(ctx,W,H,rng){const bh=30+rng()*20,sd=rng()*9999|0;pixelFill(ctx,W,H,(_,__,nx,ny)=>{const v=fbm(nx,ny,sd,3,2),rip=Math.sin(ny*20+v*8)*.5+.5;return hsl2rgb(bh+rip*15,50+rip*20,25+(ny+v*.2)*30+rip*15);});}
export function p_water_ripple(ctx,W,H,rng){const bh=rng()*360,n=2+rng()*4|0,ctr=Array.from({length:n},()=>({x:rng()*W,y:rng()*H,f:.03+rng()*.05}));pixelFill(ctx,W,H,(x,y)=>{let v=0;for(const c of ctr)v+=Math.sin(Math.sqrt((x-c.x)**2+(y-c.y)**2)*c.f*Math.PI*2);v=v/n*.5+.5;return hsl2rgb((bh+v*40)%360,70,20+v*40);});}
export function p_aurora(ctx,W,H,rng){const bh=120+rng()*80,sd=rng()*9999|0;ctx.fillStyle='#030308';ctx.fillRect(0,0,W,H);for(let band=0;band<8;band++){const yBase=H*(.1+band*.1),amp=20+rng()*40,h=(bh+band*20)%360,bH=20+rng()*30;for(let x=0;x<W;x++){const wave=fbm(x/W,band*.1,sd,4,3)*amp,g=ctx.createLinearGradient(0,yBase+wave,0,yBase+wave+bH);g.addColorStop(0,`hsla(${h},90%,60%,0)`);g.addColorStop(.4,`hsla(${h},90%,70%,0.35)`);g.addColorStop(1,`hsla(${h},80%,50%,0)`);ctx.fillStyle=g;ctx.fillRect(x,yBase+wave,1,bH);}}}
export function p_clouds(ctx,W,H,rng){const bh=rng()*360,sky=ctx.createLinearGradient(0,0,0,H);sky.addColorStop(0,`hsl(${(bh+200)%360},70%,30%)`);sky.addColorStop(1,`hsl(${(bh+220)%360},60%,50%)`);ctx.fillStyle=sky;ctx.fillRect(0,0,W,H);for(let i=0;i<12+rng()*10|0;i++){const cx=rng()*W,cy=H*(.1+rng()*.6),r=30+rng()*80;for(let b=0;b<3+rng()*3|0;b++){const bx=cx+(rng()-.5)*r*.8,by=cy+(rng()-.5)*r*.3,br=r*(.4+rng()*.6),g=ctx.createRadialGradient(bx,by-br*.2,0,bx,by,br);g.addColorStop(0,'rgba(255,255,255,0.7)');g.addColorStop(1,'rgba(255,255,255,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(bx,by,br,0,Math.PI*2);ctx.fill();}}}
export function p_lava_lamp(ctx,W,H,rng){const bh=rng()*360,bg=ctx.createLinearGradient(0,0,0,H);bg.addColorStop(0,`hsl(${(bh+180)%360},80%,15%)`);bg.addColorStop(1,`hsl(${(bh+200)%360},80%,8%)`);ctx.fillStyle=bg;ctx.fillRect(0,0,W,H);for(let i=0;i<5+rng()*6|0;i++){const cx=rng()*W,cy=rng()*H,rx=40+rng()*80,ry=50+rng()*100,h=(bh+i*40)%360,g=ctx.createRadialGradient(cx,cy,0,cx,cy,Math.max(rx,ry));g.addColorStop(0,`hsla(${h},100%,70%,0.9)`);g.addColorStop(.5,`hsla(${h},90%,50%,0.6)`);g.addColorStop(1,`hsla(${h},80%,30%,0)`);ctx.fillStyle=g;ctx.save();ctx.translate(cx,cy);ctx.scale(rx/Math.max(rx,ry),ry/Math.max(rx,ry));ctx.beginPath();ctx.arc(0,0,Math.max(rx,ry),0,Math.PI*2);ctx.fill();ctx.restore();}}
export function p_frost(ctx,W,H,rng){const bh=190+rng()*40;ctx.fillStyle=`hsl(${(bh+180)%360},40%,15%)`;ctx.fillRect(0,0,W,H);function fb(x,y,ang,len,d){if(len<2||d>12)return;const x2=x+Math.cos(ang)*len,y2=y+Math.sin(ang)*len;ctx.strokeStyle=`hsla(${bh+d*5},80%,${70+d*2}%,${.6+d*.05})`;ctx.lineWidth=Math.max(.5,d*.3);ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x2,y2);ctx.stroke();fb(x2,y2,ang,len*.75,d+1);if(rng()<.6){fb(x2,y2,ang+Math.PI/3,len*.5,d+1);fb(x2,y2,ang-Math.PI/3,len*.5,d+1);}}for(let i=0;i<3+rng()*4|0;i++){const ang=rng()*Math.PI*2;for(let k=0;k<6;k++)fb(rng()*W,rng()*H,ang+k*Math.PI/3,20+rng()*20,0);}}
export function p_feathers(ctx,W,H,rng){const bh=rng()*360;ctx.fillStyle=`hsl(${(bh+180)%360},50%,10%)`;ctx.fillRect(0,0,W,H);for(let fi=0;fi<4+rng()*4|0;fi++){const cx=rng()*W,cy=rng()*H,ang=-Math.PI/2+rng()*.4-.2,len=80+rng()*80,barbs=15+rng()*10|0,h=(bh+fi*50)%360;ctx.strokeStyle=`hsl(${h},70%,60%)`;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(cx+Math.cos(ang)*len,cy+Math.sin(ang)*len);ctx.stroke();for(let b=0;b<barbs;b++){const t=b/barbs,bx=cx+Math.cos(ang)*len*t,by=cy+Math.sin(ang)*len*t,bl=len*(.3-t*.2);ctx.strokeStyle=`hsla(${(h+t*30)%360},80%,${50+t*20}%,0.7)`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(bx,by);ctx.lineTo(bx+Math.cos(ang+Math.PI/4+t*.2)*bl,by+Math.sin(ang+Math.PI/4+t*.2)*bl);ctx.stroke();ctx.beginPath();ctx.moveTo(bx,by);ctx.lineTo(bx+Math.cos(ang-Math.PI/4-t*.2)*bl,by+Math.sin(ang-Math.PI/4-t*.2)*bl);ctx.stroke();}}}
export function p_scales(ctx,W,H,rng){const bh=rng()*360,sz=25+rng()*20|0;ctx.fillStyle=`hsl(${bh},60%,10%)`;ctx.fillRect(0,0,W,H);const rows=H/sz+2|0,cols=W/sz+2|0;for(let r=rows;r>=0;r--)for(let c=0;c<cols;c++){const x=c*sz+(r%2)*sz/2-sz,y=r*sz*.7-sz,h=(bh+c*10+r*8)%360,g=ctx.createRadialGradient(x,y-sz*.2,0,x,y,sz*.55);g.addColorStop(0,`hsl(${h},80%,65%)`);g.addColorStop(1,`hsl(${h},70%,25%)`);ctx.fillStyle=g;ctx.beginPath();ctx.ellipse(x,y,sz*.55,sz*.6,0,0,Math.PI*2);ctx.fill();}}
export function p_leaf_veins(ctx,W,H,rng){const bh=90+rng()*60;ctx.fillStyle=`hsl(${bh},60%,10%)`;ctx.fillRect(0,0,W,H);function vein(x,y,ang,len,d){if(len<2||d>9)return;const x2=x+Math.cos(ang)*len,y2=y+Math.sin(ang)*len;ctx.strokeStyle=`hsla(${(bh+d*10)%360},70%,${40+d*5}%,${.8-d*.05})`;ctx.lineWidth=Math.max(.4,d*.4);ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x2,y2);ctx.stroke();vein(x2,y2,ang,len*.8,d+1);if(rng()<.5)vein(x2,y2,ang+.4+rng()*.3,len*.5,d+1);if(rng()<.5)vein(x2,y2,ang-.4-rng()*.3,len*.5,d+1);}vein(W/2,H*.9,-Math.PI/2,H*.4,0);}
export function p_terrain(ctx,W,H,rng){const sd=rng()*9999|0,bh=rng()*360;pixelFill(ctx,W,H,(_,__,nx,ny)=>{const e=(fbm(nx,ny,sd,6,2)+fbm(nx*.5+100,ny*.5,sd+99,4,1)*.3)/.8;if(e<.35)return hsl2rgb((bh+220)%360,70,20+e*30);if(e>.8)return hsl2rgb(0,0,82+e*15);return hsl2rgb((bh+e*60)%360,55+e*25,22+e*38);});}
export function p_oil_slick(ctx,W,H,rng){const sd=rng()*9999|0,bh=rng()*360;pixelFill(ctx,W,H,(_,__,nx,ny)=>{const v=fbm(nx,ny,sd,5,3),v2=fbm(nx+5,ny+3,sd+1,5,3),irid=Math.sin((v+v2)*Math.PI*8)*.5+.5;return hsl2rgb((bh+irid*360)%360,80+irid*20,25+irid*45);});}

